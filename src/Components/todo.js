import React from 'react';
import todos from './api/todos';
import User from './user';

const Todo = ({ title, completed }) => (
  <User />,
  {title},
  {completed}
);

export default Todo;
