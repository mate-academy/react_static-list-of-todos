// Don't forget to import the React library

// TodoList component should accepts an array of preparedTodos and rendering them as a list

// Add a default export statement for TodoList component to use it in the other files
