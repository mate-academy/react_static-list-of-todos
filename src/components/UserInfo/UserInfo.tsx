// Don't forget to import the React library

// UserInfo should accepts a user object and use it to render
// a todo.user in the list with some styling. 
// (Show at least a name and an email of the user)

// Add a default export statement for UserInfo component to use it in the other files